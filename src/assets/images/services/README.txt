Service photography is stored under:

  public/images/services/

See public/images/services/SERVICES_IMAGES.txt for the exact filenames.

This folder exists so `src/assets/images/services/` is discoverable in the repo; add optional bundled assets here only if you prefer Vite imports instead of public URLs.
